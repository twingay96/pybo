from .base import *

ALLOWED_HOSTS = []

# python manage.py runserver --settings=config.local
'''
--settings 옵션은 장고서버가 읽어야 할 설정파일을 지정하는 옵션이다.
--settings=config.settings.local의 의미는 장고 서버의 설정파일로 config\settings\local.py 파일을 읽어라는 뜻이다.
'''
#
